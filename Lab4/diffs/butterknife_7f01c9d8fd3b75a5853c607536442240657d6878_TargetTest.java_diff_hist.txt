diff --git a/butterknife/src/it/field-visibility/src/test/java/test/TargetTest.java b/butterknife/src/it/field-visibility/src/test/java/test/TargetTest.java
new file mode 100644
index 0000000..a0f4d97
--- /dev/null
+++ b/butterknife/src/it/field-visibility/src/test/java/test/TargetTest.java
@@ -0,0 +1,18 @@
+package test;
+
+import butterknife.Views;
+import org.junit.Test;
+import org.junit.runner.RunWith;
+
+import static org.fest.assertions.api.Assertions.assertThat;
+
+@RunWith(ButterKnifeTestRunner.class)
+public class TargetTest {
+  @Test public void doIt() {
+    Target target = new Target();
+    Views.inject(target);
+    assertThat(target.thing1).isNotNull();
+    assertThat(target.thing2).isNotNull();
+    assertThat(target.thing3).isNotNull();
+  }
+}
