diff --git a/dist/alpine.js b/dist/alpine.js
index 9bc0a908..7fdae065 100644
--- a/dist/alpine.js
+++ b/dist/alpine.js
@@ -540,6 +540,10 @@
         } else {
           el.checked = !!value;
         }
+
+        if (typeof value === 'string') {
+          el.value = value;
+        }
       } else if (el.tagName === 'SELECT') {
         updateSelect(el, value);
       } else {
