diff --git a/openai/src/chatgpt/model/mod.rs b/openai/src/chatgpt/model/mod.rs
index 921077c..c5d1c91 100644
--- a/openai/src/chatgpt/model/mod.rs
+++ b/openai/src/chatgpt/model/mod.rs
@@ -16,6 +16,8 @@ pub enum Role {
     Assistant,
     /// A message sent by the user
     User,
+    /// A system message
+    Critic,
 }
 
 impl ToString for Role {
@@ -24,6 +26,7 @@ impl ToString for Role {
             Role::System => "system".to_string(),
             Role::Assistant => "assistant".to_string(),
             Role::User => "user".to_string(),
+            Role::Critic => "critic".to_string(),
         }
     }
 }
