diff --git a/src/account.rs b/src/account.rs
index 76ff41e..d35ff93 100644
--- a/src/account.rs
+++ b/src/account.rs
@@ -93,11 +93,16 @@ impl AccountStore for AccountFileStore {
 pub(crate) struct Account {
     email: String,
     password: String,
+    authenticate_token: Option<openai::model::AuthenticateToken>,
 }
 
 #[allow(dead_code)]
 impl Account {
     pub(crate) fn new(email: String, password: String) -> Self {
-        Self { email, password }
+        Self {
+            email,
+            password,
+            authenticate_token: None,
+        }
     }
 }
