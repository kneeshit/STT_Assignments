diff --git a/openai/Cargo.toml b/openai/Cargo.toml
index 474e427..fe1c9e6 100644
--- a/openai/Cargo.toml
+++ b/openai/Cargo.toml
@@ -25,9 +25,30 @@ rand = "0.8.5"
 jsonwebtokens = "1"
 async-trait = "0.1.68"
 derive_builder = "0.12.0"
-uuid = "1.3.3"
+futures-util = "0.3.28"
+bytes = { version = "1.4.0", default-features = false }
 
+[dependencies.uuid]
+version = "1.3.3"
+features = [
+    "v4",                # Lets you generate random UUIDs
+    "fast-rng",          # Use a faster (but still sufficiently random) RNG
+    "macro-diagnostics", # Enable better diagnostics for compile-time UUIDs
+]
+
+[features]
+stream = []
 
 [lib]
 name = "openai"
 path = "src/lib.rs"
+
+[profile.dev]
+opt-level = 'z'
+
+[profile.release]
+lto = true
+opt-level = 'z'
+codegen-units = 1
+panic = "abort"
+strip = true
