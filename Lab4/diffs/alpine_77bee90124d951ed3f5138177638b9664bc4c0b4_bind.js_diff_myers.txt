diff --git a/src/directives/bind.js b/src/directives/bind.js
index ad5c88da..67d696e5 100644
--- a/src/directives/bind.js
+++ b/src/directives/bind.js
@@ -29,7 +29,7 @@ export function handleAttributeBindingDirective(component, el, attrName, express
         }
     } else if (attrName === 'class') {
         if (Array.isArray(value)) {
-            const originalClasses = el.__originalClasses || []
+            const originalClasses = el.__x_original_classes || []
             el.setAttribute('class', arrayUnique(originalClasses.concat(value)).join(' '))
         } else if (typeof value === 'object') {
             Object.keys(value).forEach(classNames => {
@@ -40,7 +40,7 @@ export function handleAttributeBindingDirective(component, el, attrName, express
                 }
             })
         } else {
-            const originalClasses = el.__originalClasses || []
+            const originalClasses = el.__x_original_classes || []
             const newClasses = value.split(' ')
             el.setAttribute('class', arrayUnique(originalClasses.concat(newClasses)).join(' '))
         }
