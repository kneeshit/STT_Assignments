diff --git a/Cargo.toml b/Cargo.toml
index f45317d..dcfbf6a 100644
--- a/Cargo.toml
+++ b/Cargo.toml
@@ -22,6 +22,11 @@ openai = { path = "openai" }
 inquire = { version = "0.6.2", features = ["editor"] }
 colored_json = "3.2.0"
 
+[target.'cfg(windows)'.dependencies.windows-sys]
+version = "0.48.0"
+default-features = false
+features = ["Win32_System_Com_CallObj", "Win32_Foundation", "Win32_Globalization", "Win32_UI_Shell_Common"]
+
 [target.'cfg(target_family = "unix")'.dependencies]
 daemonize = "0.5.0"
 nix = { version = "0.26.2", features = ["signal", "user", "ptrace"]}
