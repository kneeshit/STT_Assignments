diff --git a/src/index.js b/src/index.js
index 2f1501b0..edcda69a 100644
--- a/src/index.js
+++ b/src/index.js
@@ -88,7 +88,14 @@ const Alpine = {
 
 if (! isTesting()) {
     window.Alpine = Alpine
+
+    if (window.deferLoadingAlpine) {
+        window.deferLoadingAlpine(() => {
+            window.Alpine.start()
+        })
+   } else {
         window.Alpine.start()
    }
+}
 
 export default Alpine
