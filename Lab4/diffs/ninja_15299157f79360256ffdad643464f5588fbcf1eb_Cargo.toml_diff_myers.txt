diff --git a/openai/Cargo.toml b/openai/Cargo.toml
index 600656f..27fd372 100644
--- a/openai/Cargo.toml
+++ b/openai/Cargo.toml
@@ -26,16 +26,9 @@ derive_builder = "0.12.0"
 jsonwebtokens = "1"
 sha2 = "0.10.6"
 hmac = { version = "0.12.1", optional = true }
-rustls-pemfile = { version = "1.0.2", optional = true }
-tokio-rustls = { version = "0.23.4", optional = true }
 futures-core = { version = "0.3.5", optional = true}
 tera = { version = "1.19.0", default-features = false, optional = true }
 
-# actix
-actix-web = { version = "4.3.1", features = ["rustls"], optional = true }
-actix-cors = { version = "0.6.4", optional = true }
-static-files = { version = "0.2.3", optional = true }
-
 # redis limit
 redis = { version = "0.23.0", features = ["tokio-comp", "cluster-async"], optional = true }
 redis-macros = { version = "0.2.1", optional = true }
@@ -57,12 +50,22 @@ hex = "0.4.3"
 ring = { version = "0.16.20", features = ["std"] }
 cbc = "0.1.2"
 
+# axum
+axum = { version = "0.6.19", features = ["http2"], optional = true }
+axum-extra ={ version = "0.7.5", features = ["cookie"], optional = true }
+axum-server = { version = "0.5.1", default-features = false, features = ["tls-rustls"], optional = true }
+tower-http = { version = "0.4.3", default-features = false, features = ["cors", "trace"], optional = true }
+tower = { version = "0.4.13", default-features = false, features = ["limit", "timeout"], optional = true}
+bytes = { version = "1.4.0", optional = true }
+time = { version =  "0.3.23", optional = true }
+static-files = { version = "0.2.3", optional = true }
+
 [build-dependencies]
 static-files = "0.2.3"
 
 [features]
 default = ["serve", "limit", "sign", "opengpt", "platform", "stream", "template"]
-serve = ["dep:actix-web", "dep:actix-cors", "dep:static-files", "dep:rustls-pemfile", "dep:tokio-rustls", "dep:futures-core", "dep:tera"]
+serve = ["dep:tower-http", "dep:tower", "dep:bytes", "dep:time", "dep:axum-server", "dep:axum-extra", "dep:axum", "dep:static-files", "dep:futures-core", "dep:tera"]
 opengpt = []
 platform = ["dep:tokio-util"]
 stream = ["dep:futures", "dep:tokio-stream", "dep:eventsource-stream", "dep:futures-core", "dep:pin-project-lite", "dep:nom", "dep:mime", "dep:futures-timer"]
