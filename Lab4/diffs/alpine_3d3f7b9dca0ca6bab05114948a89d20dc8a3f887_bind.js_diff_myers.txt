diff --git a/src/directives/bind.js b/src/directives/bind.js
index a2b6a089..ca59a083 100644
--- a/src/directives/bind.js
+++ b/src/directives/bind.js
@@ -19,6 +19,12 @@ export function handleAttributeBindingDirective(component, el, attrName, express
                 el.checked = el.value == value
             }
         } else if (el.type === 'checkbox') {
+            // If we are explicitly binding a string to the :value, set the string,
+            // If the value is a boolean, leave it alone, it will be set to "on"
+            // automatically.
+            if (typeof value === 'string' && attrType === 'bind') {
+                el.value = value
+            }else if (attrType !== 'bind') {
                 if (Array.isArray(value)) {
                     // I'm purposely not using Array.includes here because it's
                     // strict, and because of Numeric/String mis-casting, I
@@ -34,11 +40,6 @@ export function handleAttributeBindingDirective(component, el, attrName, express
                 } else {
                     el.checked = !!value
                 }
-            // If we are explicitly binding a string to the :value, set the string,
-            // If the value is a boolean, leave it alone, it will be set to "on"
-            // automatically.
-            if (typeof value === 'string') {
-                el.value = value
             }
         } else if (el.tagName === 'SELECT') {
             updateSelect(el, value)
